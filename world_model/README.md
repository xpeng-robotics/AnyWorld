# AnyWorld world-model inference

This directory contains the FastVideo-based inference code used by AnyWorld's
Wan2.1 14B action-camera-embodiment model. Use the repository-level README for
installation, input manifests, model weights, and inference. The code in this
directory is distributed under Apache-2.0 and retains upstream SPDX headers.
